<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".PdfApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PDFToolsPro"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PDFToolsPro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".features.compress.CompressActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".features.merge.MergeActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".features.convert.ConvertActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".features.rotate.RotateActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".features.viewer.ViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />
            
        <activity
            android:name=".ui.subscription.SubscriptionActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFToolsPro" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
